# --- Core Orchestration ---
python-dotenv==1.0.1
requests==2.32.3
boto3==1.34.0

# --- Big Data Engine ---
pyspark==4.1.1
findspark==2.0.1        # CRITICAL: Fixes the 'NoneType' origin error we saw earlier
pyarrow==15.0.0         # REQUIRED: For Snappy-compressed Parquet support

# --- NoSQL Serving Layer ---
happybase==1.2.0
thrift==0.20.0          # REQUIRED: Underlying protocol for HBase communication

# --- Data Manipulation ---
pandas==2.2.0
numpy==1.26.4